<% etiquetas.forEach(function(et){ %>
  <div class="container">
    <img class="etiqueta" src="/images/ofertonSgl.png" alt="">
    <div>
      <%= et.BARCODE %>
    </div>
    <div>11509</div>
    <div>
      <%= (et.PUNIT + '' ).split('.')[0] %>
        <span>
          <%= (et.PUNIT + '' ).split('.')[1] %>
        </span>

    </div>
    <div>
      <%= et.DESCRIP %>
    </div>
  </div>
  <% }); %>